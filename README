Qt and QML bindings for Grilo.
==============================

Qt4, Qt5 and QML declarative bindings for Grilo.
Grilo is a framework for discovering and browsing media.

Requirements:
=============
grilo >= 0.2.0

Installation:
=============
qmake && make && make install
